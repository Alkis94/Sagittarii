public enum RoomType
{
    //Rooms must be assigned in  Rooms list in this exact order!

    noRoom,
    horizontalRoad,
    verticalRoad,
    exploredRoom,
    unexploredRoom,
    bossRoom,
    treasureRoom,
    challengeRoom
}